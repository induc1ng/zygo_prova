    <body>
        <p id="notice"><%= notice %></p>

        <p>
            <strong>Title:</strong>
            <%= @book.title %>
        </p>

        <p>
            <strong>Author:</strong>
            <%= @book.author %>
        </p>

        <p>
            <strong>Description:</strong>
            <%= @book.description %>
        </p>

        <p>
            <strong>Image:</strong>
            <%= @book.image %>
        </p>

        <% if current_user.admin == 1 %> <%= link_to 'Edit', edit_book_path(@book) %> | <%end%>
        <%= link_to 'Back', books_path %>
    </body>